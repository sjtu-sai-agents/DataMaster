## Reference Code

The following reference code has already been verified to be effective:

```python
{initial_code_content}
```

## Your Task

You are given a reference solution that is already known to be valid. Your job in this node is to convert the user-provided initial code into the standard format used by this system, and produce the **first complete valid submission**.

This node is for faithful refactoring only.

You MUST:
- preserve the original algorithm
- preserve the original preprocessing behavior
- preserve the original data usage
- preserve the original validation logic if already correct

You MUST NOT:
- optimize the model
- add new data
- redesign the pipeline
- intentionally change behavior
- explore alternative approaches
